// expect: 99
//
// tests/37_globals.c0 - globals: cross-function state, negative initializer, local shadowing
// Copyright (C) 2026 tavro. Part of coff, GPL-3.0-or-later (see ../LICENSE).
int counter = 0;
int offset = -5;

int bump() {
    counter = counter + 1;
    return counter;
}

int main() {
    bump();
    bump();
    bump();
    int counter = 100;   // local shadows the global
    return counter + bump() + offset;
}
